node_modules/
.env
.env.local
.env*.local
*.log
.DS_Store
.env.production

# Next.js
.next/
out/
next-env.d.ts.bak

# TypeScript
*.tsbuildinfo

# Vercel
.vercel

# Reference-only — buddy's Evernude attribution code, kept locally for lookup
_reference-evernude/

# Local-only files (Claude Code config + planning docs)
.claude/
Claude Conversation.docx
docs/

# SSH keys must never be tracked (defense in depth — they should live in ~/.ssh)
id_ed25519*
*.pem
*.key
